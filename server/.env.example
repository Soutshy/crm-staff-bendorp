# ===== Server Env =====
PORT=4000
# Neon connection string, e.g. postgres://user:pass@host/db?sslmode=require
DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DB_NAME?sslmode=require
# JWT secret (change in production)
JWT_SECRET=super-secret-change-me
# Allowed client origin (for CORS), e.g. http://localhost:5173
CLIENT_ORIGIN=http://localhost:5173
# Optional: allow running seed endpoint/script to create initial SUPER_ADMIN
SEED_EMAIL=admin@bendorp.gg
SEED_PASSWORD=ChangeMe!23
SEED_DISPLAY_NAME=Super Admin
